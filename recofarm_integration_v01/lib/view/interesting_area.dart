import 'package:flutter/material.dart';

class InterArea extends StatelessWidget {
  const InterArea({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('나의 관심 지역'),
      ),
      body: Center(),
    );
  }
}